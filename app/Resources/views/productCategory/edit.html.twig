{{ form_start(form, { 'action': path('product_category_edit', { id: id }), 'id': 'product_category_edit_form' }) }}
    <div class="form-group">
        {{ form_widget(form.name, { attr: { 'class': 'form-control', 'placeholder': 'Nom'} }) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.description, { attr: { 'class': 'form-control', 'placeholder': 'description', 'rows': '3'} }) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.submit, { attr: { 'class': 'btn btn-block btn-default'} }) }}
    </div>
{{ form_end(form) }}